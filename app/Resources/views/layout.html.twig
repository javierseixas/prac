<!doctype html>
<html lang="ca">
<head>
	<meta charset="utf-8">
	<title>Administrador Botiga</title>
	<meta name="description" content="">
	<meta name="author" content="Javier Seixas">
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/custom.css">
	<!--[if lt IE 9]>
	<script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
	<![endif]-->
	<style>
     	body {
        	padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      	}
    </style>
</head>
<body>
	
	
    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Administració botiga</a>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <li class="active-"><a href="/productes">Productes</a></li>
              <li><a href="/descripcions">Descripcions</a></li>
              <li><a href="/categories">Categories</a></li>
              <li><a href="/classificacio">Classificació</a></li>
              <li><a href="/estocs">Estocs</a></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">

          {% if silex.session.hasFlash('notice') %}
          <div class="alert alert-success in fade" style="display: inline-block">
              <a class="close" data-dismiss="alert">×</a>
              {{ silex.session.getFlash('notice') }}
          </div>
          {% endif %}

          {% if silex.session.hasFlash('error') %}
          <div class="alert alert-error in fade" style="display: inline-block">
              <a class="close" data-dismiss="alert">×</a>
              {{ silex.session.getFlash('error') }}
          </div>
          {% endif %}

          {% if silex.session.hasFlash('login-error') %}
            <div class="alert alert-error in fade" style="display: inline-block">
                <a class="close" data-dismiss="alert">×</a>
                {{ silex.session.getFlash('login-error') }}
            </div>
          {% endif %}
          {% if silex.session.hasFlash('txn-ok') %}
            <div class="alert alert-success in fade" style="display: inline-block">
                <a class="close" data-dismiss="alert">×</a>
                {{ silex.session.getFlash('txn-ok') }}
            </div>
          {% endif %}
    
    	{% block container %}
			<h1>Administració botiga</h1>
			<p>Benvingut a l'administració. Disposa del menú per gestionar els productes a la part superior del seu navegador.</p>
      	{% endblock %}

    </div> <!-- /container -->


	<script src="/bootstrap/js/bootstrap.min.js"></script>
	<script src="/js/scripts.js"></script>
</body>
</html>